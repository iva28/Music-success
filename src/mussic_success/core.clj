(ns mussic-success.core
  (:gen-class)
  (:require [menu]))

;(defn list-all-namespaces []
;  (println "All namespaces in the project:")
;  (doseq [ns (find-namespaces-in-dir (io/file "src"))]
;    (println ns)))
;;
;(defn -main
;  "I don't do a whole lot ... yet."
;  [& args]
;  (menu/main-menu))
;;
;;
;(-main)